

<ul>
    <% for (var i in tips) { %>
        <% var tip = tips[i]; %>

        <li>

            <% if (tip.accepted ||
                    (session.user && (session.user.id===tip.AuthorId ||(session.user.isAdmin || (quizAuthorId  && quizAuthorId === session.user.id))))) { %>

                <%= tip.text %>

            <% } %>

           <% if (tip.accepted &&
                    (session.user && (session.user.id===tip.AuthorId ||(session.user.isAdmin || (quizAuthorId  && quizAuthorId === session.user.id))))) { %>

                <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>/accept?_method=PUT"><button>Aceptar</button></a>

            <% } %>

           <% if (tip.accepted ||
                    (session.user && (session.user.id===tip.AuthorId ||(session.user.isAdmin || (quizAuthorId  && quizAuthorId === session.user.id))))) { %>

                <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>?_method=DELETE"><button>Borrar</button></a>

            <% } %>
		<div><small> (by <span id="autor"><%= tip.Author ? tip.Author.username : "Anómino" %></span>) </small>
</div>
        </li>
    <% } %>
</ul>
